- console.log('')

= DOM(['body'], {'background': 'red'});

var color = props.color || 'red'

// this is a comment

/* another comment */



.button,
.btn,
button,
form
  color =color
  yield
  - debugger
  Other(testing)
  Foobar(color=test)
    block test(color)
      color = color
    block header
      margin 10px

  = test('green', 'back-tick')

  if props.color === 'blue'
    &:active
      outline: none
  else
    color : brown

  each v, k in [1,2,3,4]
    &:nth(`v`)
      margin-left = v + 'px'
    .other
      padding 20px;

  var foo = 'red'

  @media mobile
    yield mobile(123)

function test(color, sel)
  color =color
  text-rendering optimizeLegibility
  &.test
    background =color

  .`sel`
    font helvetica

function Other()
  color red

function Foobar()
  color blue